<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:id="@+id/root"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:fitsSystemWindows="true">

	<android.support.design.widget.AppBarLayout
		android:id="@+id/appbar"
		android:layout_width="match_parent"
		android:layout_height="@dimen/home_header_height"
		android:fitsSystemWindows="true"
		android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

		<android.support.design.widget.CollapsingToolbarLayout
			android:id="@+id/collapsing_toolbar"
			android:layout_width="match_parent"
			android:layout_height="@dimen/home_header_height"
			android:fitsSystemWindows="true"
			app:contentScrim="@color/colorBlack"
			app:layout_scrollFlags="scroll|exitUntilCollapsed"
			app:titleEnabled="false">

			<ImageView
				android:id="@+id/tab_header_image"
				android:layout_width="match_parent"
				android:layout_height="match_parent"
				android:adjustViewBounds="true"
				android:scaleType="centerCrop"
				android:fitsSystemWindows="true"
				app:layout_collapseMode="parallax"/>

			<!-- image overlay -->
			<View
				android:layout_width="match_parent"
				android:layout_height="match_parent"
				android:alpha="0.66"
				android:background="@color/colorPurple"
				android:fitsSystemWindows="true"/>

			<android.support.v7.widget.Toolbar
				android:id="@+id/tab_toolbar"
				android:layout_width="match_parent"
				android:layout_height="@dimen/tab_strip_height"
				android:minHeight="0dp"
				android:layout_gravity="top"
				app:layout_collapseMode="pin"
				app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

			<com.gigamole.navigationtabstrip.NavigationTabStrip
				android:id="@+id/tab_strip"
				android:minHeight="@dimen/tab_strip_height"
				android:layout_width="match_parent"
				android:layout_height="@dimen/tab_strip_height"
				android:layout_gravity="bottom"
				android:fitsSystemWindows="true"
				app:nts_active_color="@color/colorWhite"
				app:nts_inactive_color="@color/colorDark"
				app:nts_color="@color/space_time"
				app:nts_corners_radius="1dp"
				app:nts_size="15sp"
				app:nts_weight="3dp"
				/>

		</android.support.design.widget.CollapsingToolbarLayout>

	</android.support.design.widget.AppBarLayout>

	<!-- Likes and Dislikes Frags -->
	<android.support.v4.view.ViewPager
		android:id="@+id/stash_viewpager"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

</android.support.design.widget.CoordinatorLayout>